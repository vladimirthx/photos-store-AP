# Frontend - Copy this file as .env and fill in the values
# Vite requires the VITE_ prefix for variables exposed to the browser

# In development use localhost, in production use DigitalOcean service URLs
VITE_AUTH_URL=http://localhost:3001
VITE_CATALOG_URL=http://localhost:3002
VITE_PAYMENT_URL=http://localhost:3003
